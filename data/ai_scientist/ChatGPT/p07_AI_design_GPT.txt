Paper p07 — Experimental Design Description
1. Experimental Objective

To evaluate whether AgriBERT, a pretrained language model for agricultural text, improves performance on agricultural NLP tasks such as question answering and named entity recognition.

2. Dataset Description

Source: Agricultural text datasets for QA and NER.
Size: Not specified in metadata
Type: Agricultural domain text corpora used for pretraining and downstream QA/NER tasks.
Preprocessing: Not specified in metadata

3. Model Description

Model: AgriBERT, a domain-pretrained BERT model tailored for agricultural language.
Training approach:

Pretraining on agricultural corpora (details not provided).

Fine-tuning on QA and NER tasks.
Baselines: Baseline LMs (names not specified).

4. Experimental Procedure

Data Preparation

Collect agricultural corpora for pretraining.

Prepare QA and NER datasets for fine-tuning (details not specified).

Model Training

Pretrain AgriBERT on agricultural-domain text.

Fine-tune AgriBERT on downstream QA and NER datasets.

Inference

Evaluate AgriBERT on agricultural QA and NER tasks.

Compare outputs with baseline language models.

5. Evaluation Setup

Metrics: F1 score, Exact Match (EM).
Baselines: Baseline LMs (specific models not listed).
Test dataset: Not specified in metadata

6. Strengths

Domain-specific pretraining tailored to agricultural text.

Evaluated on multiple downstream tasks (QA + NER).

Uses standard NLP metrics (F1, EM).

Shows clear performance improvement (+8% EM per metadata).

7. Limitations

Dataset sizes and preprocessing steps are not described.

Exact baseline models not identified.

Training details (epochs, batch size, hardware) missing.

No human evaluation.

Test set composition not provided.

